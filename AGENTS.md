# AGENTS.md

## Cursor Cloud specific instructions

This repository (`mf-cli`) is currently an empty scaffold with only a `README.md`. There are no source files, dependencies, build systems, tests, or services to run.

- **No package manager lockfile** exists — once code is added, check for `package-lock.json` / `yarn.lock` / `pnpm-lock.yaml` / `requirements.txt` / `go.mod` etc. and install accordingly.
- **No lint, test, or build commands** are configured yet.
- Future agents should re-evaluate this file once the codebase has actual code.
